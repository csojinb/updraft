[tox]
envlist = flake8, py27

[testenv]
deps = -r{toxinidir}/tools/test-requires
commands =
    coverage run -m py.test {toxinidir}/tests
    coverage combine
    coverage report -m

[testenv:flake8]
commands =
    flake8 --statistics -j auto --count updraft tests
